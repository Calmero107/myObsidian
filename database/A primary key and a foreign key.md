
**Primary key (primary code)** : The primary key is used to uniquely identify a tuple. It cannot be repeated or empty. A table can only have ==one primary key==.

**Foreign key (foreign code)** : Foreign keys are used to establish connections with other tables. Foreign keys are the primary keys of another table. Foreign keys can be repeated and can be null. A table can have ==multiple foreign keys==.