
Attributes (corresponding to fields in the table) cannot be split any further, which means that this field can only have one value and cannot be divided into multiple other fields. **1NF is the most basic requirement for all relational databases** , which means that the tables created in a relational database must meet the first normal form.

**Mỗi thuộc tính của bảng phải chứa một giá trị duy nhất.**

![[Pasted image 20241226094517.png]]