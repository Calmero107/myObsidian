
Uniform Resource Locators
All resources on the Internet are located by URLs. Each file corresponds to a URL, just like a path address. In theory, file resources and URLs correspond one to one. In practice, there are exceptions. For example, the files pointed to by some URLs have been relocated to another location, so there are multiple URLs pointing to the same file.